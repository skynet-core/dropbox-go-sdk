# Unofficial DropBox SDK for Go

## How to generate the code

1. Fetch submodules `git submodule update --init --recursive`
2. Install [stone](https://github.com/dropbox/stone) `pip install --user stone` (Python3 and pip are required)
3. Generate the code `go run ./generator/generate.go`


## Usage